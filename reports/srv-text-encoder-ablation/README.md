`accuarcy_by_entanglement_bucket.png` and `summary_comparison.png` have been created with 8192 samples per bucket, exactly like in the paper.
`summary.csv` shows, that a CLOOB text encoder, no matter which size (RN50 or RN50x4) outperforms the baseline.
